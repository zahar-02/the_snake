
# Cкажу честно я не до конца понял как работает программа ,но я расберусь !!!


class GameObject:


    def __init__(self, posicion=None, body_color=None):
        

        if posicion is None:
            
        else:

            self.posicion = posicion


    def draw(self. surfaec):

class Apple(GameObject):



    super().__init__(position=None, body_color=APPLE=COLOR)

    self.randomize_position()

    def randomize_position(self):
        max_x = 640 - 20
        max_y = 480 - 20 
        x.random.randrage(0, max_x + 1, 20)
        y.random.randrage(0, max_x + 1, 20)

        self.posicion = (x, y)

    def draw(self, surface):
        
        rect = pygame.Rect(
             self.position[0],
             self.position[1],
             20,
             20,
        )

        pygame.draw.rect(surface, self.body_color, rect)



# Cкажу честно я не до конца понял как она работает,но я разберУ !!!






